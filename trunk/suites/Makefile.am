if WANT_DNS
  DNS_OPT = dns
endif
if WANT_DISK
  DISK_OPT = disk
endif
if WANT_VST
  VST_OPT = vst
endif
if WANT_MULTI
  MULTI_OPT = multi
endif

SUBDIRS =  $(DNS_OPT) $(DISK_OPT) $(VST_OPT) $(MULTI_OPT)

